# Скопировать в workers/.dev.vars (в git не попадает).
#
# Локально годится любая строка: тесты подписывают initData этим же значением.
# Для отладки в живом Telegram нужен настоящий токен из BotFather — иначе
# валидация initData не сойдётся.
BOT_TOKEN=local-dev-bot-token
