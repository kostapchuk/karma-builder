# V2 — состояние на 17 августа 2026

Что построено по [плану V2](./v2-reviews.md), чем реализация от него отличается
и что осталось сделать руками.

Документ описывает **фактическое** состояние кода. План остаётся замыслом
и под реализацию не переписывался — расхождения перечислены явно, как и в
[v1-status.md](./v1-status.md).

---

## Что готово

Все семь пунктов «Порядка реализации» из плана.

| Часть | Где |
|---|---|
| Схема D1 | `workers/migrations/0001_init.sql` |
| Валидация initData | `workers/src/auth/telegramAuth.ts` |
| Дела и ссылки на ревью | `workers/src/routes/deeds.ts` |
| Приём оценок и агрегация | `workers/src/routes/review.ts` |
| Лидерборды и профиль друга | `workers/src/routes/leaderboard.ts` |
| Друзья через deep-link | `workers/src/routes/friends.ts` |
| Импорт истории V1 | `workers/src/routes/importLegacy.ts` |
| Страница проверяющего (SPA) | `reviewer/` — позже удалена, см. ниже |
| Mini App поверх API | `app/`, `lib/api/`, `lib/store/` |

Ядро кармы из V1 (`lib/karma/*`) переиспользуется Worker'ом **как есть** —
ровно так, как обещал план: ни одной копии весов, кривой уровней или условий
выдачи бейджей на сервере не появилось.

---

## Расхождения с планом

Семь отступлений. Первые три меняют схему, остальные — механику.

**1. В `users` добавлены агрегаты: стрик, счётчики, бейджи.**
План описывал только карму и уровень. Но экраны V2 показывают стрик и бейджи
(`/api/me`, профиль друга), а считать их запросом по всей истории на каждый
вызов дороже, чем держать в строке юзера. Раскладка повторяет `state` из V1:
`streak_current`, `streak_longest`, `last_deed_date`, `deed_count`,
`category_counts`, `badges`.

**2. У дела появился `local_date`.**
Worker живёт в UTC, «день» для стрика и heatmap — календарный день
пользователя. Без отдельной колонки они разъезжались бы на границе суток.
Клиент присылает свой день вместе с делом.

**3. Флаг импорта — на сервере, а не в `meta` CloudStorage.**
План предлагал пометить `meta` ключом `imported`. Вместо этого стоит
`users.legacy_imported_at`. Причина: флаг на устройстве не защищает от второго
устройства — пользователь открыл бы V2 на планшете и импортировал бы всё ещё
раз. Серверный флаг закрывает оба случая, и `meta` в V1-хранилище остаётся
нетронутым.

**4. Одноразовость токена держится на `INSERT ... SELECT`, а не на «UPDATE, потом INSERT».**
План предлагал сначала гасить токен, потом писать оценку. Тогда между двумя
операциями остаётся щель: если `UPDATE` не нашёл свободного токена, `INSERT`
всё равно выполняется, и спасает только `UNIQUE`. Сейчас условие «токен ещё
свободен» встроено в саму вставку:

```sql
INSERT INTO reviews (...) SELECT deed_id, reviewer_slot, ?, ?, ?
  FROM review_tokens WHERE token = ? AND used_at IS NULL AND ...
```

Ноль вставленных строк = кто-то успел раньше, и ответ 410. Гашение токена идёт
следом в той же транзакции. Проверено тестом с двумя одновременными сабмитами.

**5. Агрегация условна по статусу дела — иначе карма начислялась бы дважды.**
Плана «когда проходит второй INSERT» недостаточно: обе гонящиеся записи могут
увидеть «оценок стало две» одновременно. Поэтому начисление кармы и перевод
дела в `approved` идут одним батчем, и **оба** условны по прежнему статусу
дела — применится ровно одна пара. Уровень при этом считается от прочитанного
значения кармы: при совсем одновременных аппрувах он отстанет на шаг и
починится на следующем, а наружу всё равно считается из кармы.

**6. Перегенерация ссылки заменяет строку токена, а не добавляет новую.**
`UNIQUE(deed_id, reviewer_slot)` не даёт держать два токена на слот, поэтому
`send-review` делает `ON CONFLICT DO UPDATE`: старый токен перестаёт
существовать в ту же секунду. Живой токен переиспользуется, а не выдаётся
заново — иначе каждая отправка ломала бы уже разосланную ссылку.

**7. Бейджи при импорте считаются по сумме обеих шкал.**
Иначе бейджи за уровень («Хранитель», «Просветлённый») исчезли бы при переезде:
подтверждённая карма у мигранта нулевая. Бейджи — личные достижения, они не
влияют на рейтинг, и терять их незачем. На лидерборд это не влияет: там только
`karma_total`.

---

## Дев-режим: initData теперь подписывается по-настоящему

В V1 дев-мок подставлял `hash: 'devmode'` — проверять было нечем. В V2 Worker
валидирует HMAC на каждый запрос, и такой мок означал бы 401 на весь дев-режим.

Теперь `lib/telegram/init.ts` подписывает дев-initData локальным токеном
(тем же, что в `workers/.dev.vars`), и в браузере работает ровно тот же путь,
что в Telegram. Две детали, на которых это ломалось:

- **Поле `signature` обязательно**: без него SDK вообще не принимает
  `tgWebAppData`. На проверку HMAC оно не влияет — Worker пробует обе раскладки
  data-check-string, с ним и без него.
- **Строку нужно отдавать ровно такой, какой её подписали.** SDK разбирает
  initData в объект и собирает обратно сам; любая пересборка JSON юзера ломает
  HMAC. Поэтому подписанная строка запоминается в `devInitData()`, и клиент
  API в дев-режиме берёт её, а не то, что вернёт SDK.

`initTelegram()` из-за подписи стала асинхронной и возвращает один общий
промис: в дев-режиме React вызывает эффекты дважды, и второй вызов не должен
отвечать «готово» раньше, чем закончился первый.

---

## Что проверено

**Юнит-тестами** (`npm run test:api`, 22 проверки):

- валидация initData: подделанный hash, подмена `user` под чужой подписью,
  чужой бот, протухший `auth_date`, поле `signature`;
- пересчёт прогресса: счётчики, стрик, бейджи при создании дела и при аппруве;
- среднее двух оценок и округление половины вверх;
- восстановление стрика из истории V1 и слияние с текущим;
- алфавит токена ровно 64 символа и токены без повторов.

Последний тест появился не зря: в алфавите не хватало одного символа, маска
`byte & 63` вылезала за границу, и в токенах посреди строки оказывалось
`undefined`. Поймано на живой странице, а не тестом.

**End-to-end** (`npm run test:e2e`, 21 сценарий на живом `wrangler dev` + D1) —
всё, что перечислено в разделе «Верификация» плана, плюс:

- гонка двух сабмитов по одной ссылке → ровно одна запись в `reviews`;
- гонка двух последних оценок → карма начисляется один раз;
- чужое дело не открывается и ссылок на него не выдаётся;
- профиль друга не отдаёт `karma_self_total` и `telegram_id`;
- инвариант базы после прогона: карма каждого юзера равна сумме итоговых
  баллов его подтверждённых дел.

**В браузере** (Mini App на `next dev` + Worker + страница проверяющего):

- запись дела → две ссылки → оценки 20 и 30 от «посторонних» → `+25`
  подтверждённой кармы в приложении;
- одноразовость: по использованной ссылке страница показывает «уже оценили»;
- импорт V1: 3 дела из localStorage → `+38` самооценки, подтверждённая карма
  не изменилась, дела помечены «Из прежней версии», повторный запуск ничего
  не задвоил;
- фолбэк share: вне Telegram ссылка кладётся в буфер и об этом сообщается.

**Не проверено:**

- живой Telegram: нативный share двух ссылок, deep-link `?startapp=f<id>`
  и добавление друга по нему;
- прод-деплой (Workers, Pages, Vercel) — ниже расписано, что для него нужно;
- два устройства под одним аккаунтом;
- поведение при сотнях дел: история грузится страницей в 50 записей,
  пагинация в UI не сделана (`hasMore` API отдаёт, экран его не использует).

---

## Чего нет намеренно

- **Rate-limit на маршрут ревью.** План допускал правила Cloudflare или счётчик
  в KV. Не сделано: KV ради MVP заводить не хотелось, а правила настраиваются
  в панели Cloudflare, а не в коде. Усреднение двух независимых оценок уже
  гасит накрутку одним человеком.
- **Cron, проставляющий `status='expired'`.** Как и планировалось, истечение
  проверяется лениво при чтении: автор видит «ссылка истекла» и жмёт
  «Создать ссылки заново». Статусы `expired` и `rejected` в схеме есть, но
  сейчас их никто не выставляет.
- **Удаление аккаунта.** Кнопка «Сбросить данные» из V1 убрана: она стирала
  CloudStorage, а данные теперь на сервере. Соответствующего endpoint'а нет.
- **Оптимистичный UI.** Запись дела — один запрос вместо трёх обращений к
  CloudStorage, пауза заметно короче, чем была в V1. Индикатор записи остался.

---

## Что развёрнуто

Прод поднят 18 августа 2026 в аккаунте Cloudflare с одним Worker-субдоменом
`xfaseex1.workers.dev`.

| Что | Адрес |
|---|---|
| API + страница проверяющего | `karma-builder-api.xfaseex1.workers.dev` |
| Mini App | `karma-builder-app.xfaseex1.workers.dev` |
| D1 `karma-builder` | регион EEUR, миграция `0001_init` применена |

`BOT_TOKEN` лежит в `wrangler secret`. `database_id` попал в
`workers/wrangler.jsonc`: он не секрет — без авторизации в аккаунт бесполезен,
а без него не собрать ни деплой, ни миграцию.

Деплой — две команды, `npm run deploy:api` и `npm run deploy:app`. Обе
пересобирают статику сами, но адрес API у Mini App вшивается на сборке, так
что её нужно именно пересобирать при смене домена API, а не передеплоивать.

---

## Почему не Pages

План селил страницу проверяющего на Cloudflare Pages, а Mini App на Vercel.
Страница на Pages уехала и не открылась: зона `pages.dev` у провайдера,
с которого ведётся разработка (Beltelecom, BY), режется на двух уровнях —
DNS отдаёт `82.209.230.73` вместо адреса Cloudflare, а с подставленным
настоящим адресом соединение рвётся на TLS-хендшейке, то есть по SNI.
`workers.dev`, `vercel.app` и остальные домены Cloudflare при этом доступны:
блокируется именно зона `pages.dev`.

Для ссылки на ревью это фатально: её открывает посторонний человек в обычном
браузере, и если он у того же провайдера — ссылка мертва, а это весь смысл V2.
Поэтому статику раздают Worker'ы:

- **страницу проверяющего отдаёт сам Worker API** (`assets` в
  `workers/wrangler.jsonc`). `/r/{token}` попадает в SPA через
  `not_found_handling: single-page-application`, а `/api/*` перехватывается
  раньше ассетов через `run_worker_first` — иначе SPA-фолбэк отдавал бы HTML
  на несуществующий маршрут API вместо JSON-404. Побочно исчезли двойной
  деплой ради обмена доменами и CORS между ними: origin стал общим;
- **Mini App раздаёт отдельный Worker без кода** (`wrangler.app.jsonc`) —
  только `assets`, промах отдаётся страницей `404.html` из экспорта, а не
  главным экраном.

`reviewer/public/_redirects` при переезде удалён: правило `/* /index.html 200`
Workers считает бесконечной петлёй и отклоняет деплой, а SPA-фолбэк теперь
делает `not_found_handling`.

Это не защита от блокировок вообще — `workers.dev` можно закрыть той же
логикой. Устойчивый ответ — свой домен; пока его нет, приложение живёт на
проверенно доступной зоне.

---

## Правки после первого дня в проде

Четыре изменения по итогам живого использования.

**1. Проверяющий опознан; ревью переехало внутрь Mini App.**
Главная дыра замысла: страница открывалась в любом браузере без логина, поэтому
сервер не знал, кто ставит балл, — и автор мог подтвердить своё же дело. На
практике это и случилось. Теперь ссылка — deep-link `?startapp=r<token>`,
маршрут `/api/review/:token` требует initData, а у оценки появился
`reviewer_user_id` (миграция `0002`). Автору своё дело не открывается, один
человек не оценивает одно дело дважды — второе держится уникальным индексом
`(deed_id, reviewer_user_id)`, а не только проверкой в коде. Отдельная SPA
`reviewer/` удалена, шкала переехала в `components/ReviewScale.tsx`; прежний
адрес `/r/<token>` Worker перенаправляет в Telegram, чтобы разосланные ссылки
не умерли.

**2. Проверяющий теперь один.**
`REVIEWER_SLOTS` в `lib/karma/review.ts` — единственный регулятор: от длины
списка зависят и число ссылок, и порог перевода в `approved`. При одном
проверяющем усреднение двух независимых оценок больше не гасит накрутку — это
осознанный размен на время закрытого запуска. Возврат к двум — правка одной
строки; e2e-набор считает ожидания от той же константы и проходит при обоих
значениях (проверено).

**3. Потолок пользователей.**
`MAX_USERS` в `vars` (сейчас 10). Ограничивает только регистрацию: условие
«мест хватает» встроено в `INSERT ... SELECT`, поэтому два одновременных первых
запуска не проскочат мимо потолка. Уже заведённые ходят дальше — их пропускает
отдельная ветка `EXISTS`, без которой закрытый набор выгонял бы всех
(поймано тестом).

**4. Шкала перестала лагать.**
Переход `left/width` на 220 мс висел постоянно, и при перетаскивании ручка
отставала от пальца на эти 220 мс. Теперь он включается только на скачках —
въезд при открытии и тычок в якорь, — а у слайдера появился `touch-action: none`,
без которого браузер сначала выяснял, не скролл ли это.

---

**5. Самооценка и импорт истории V1 убраны целиком.**
План строил две шкалы: подтверждённая карма шла в рейтинг, перенесённая из V1
висела рядом, чтобы прогресс не обнулился. На практике вторая шкала только
путала — трекается то, что подтвердил посторонний, остальное значения не имеет.
Миграция `0003` удаляет перенесённые дела и колонки `karma_self_total`
и `legacy_imported_at`, а заодно пересчитывает `deed_count`, `category_counts`,
стрик и `last_deed_date`: они считались вместе с импортом и после удаления
врали бы. Бейджи оставлены — личные достижения, отбирать уже выданное незачем
(та же логика, что была при импорте). Ушли `/api/import/legacy`,
`lib/storage/legacy.ts`, статус `legacy_unverified` и всё, что их показывало.

`CHECK` на `deeds.status` по-прежнему допускает `legacy_unverified`: снять его
в SQLite можно только пересборкой таблицы, на которую смотрят внешние ключи
`reviews` и `review_tokens`. Значение не пишет никто, поэтому лишний вариант
в перечислении безвреден.

---

**6. Награда за приглашённых.**
Пригласивший получает разовый бонус за приглашённого и долю от его
подтверждённых дел. Два места, где исходная формулировка не работала как есть:

- «+10 за переход по ссылке» — это карма без чужой оценки, ровно та накрутка,
  против которой построен V2. Бонус привязан к **первому подтверждённому делу**
  приглашённого: за ним тогда стоит живой человек и живой проверяющий;
- «1% от их дел» при целочисленной карме — ноль. Дело стоит 5–50, процент от
  него 0.05–0.5, и обычное округление съедало бы долю навсегда. Остаток копится
  в сотых (`users.referral_fraction`) и переходит в карму по достижении балла.

Инвариант базы стал длиннее: карма юзера равна сумме его подтверждённых дел
**плюс** `karma_referral`. Ради этого реферальная часть и хранится отдельной
колонкой, хотя в `karma_total` входит целиком — второй шкалы в интерфейсе нет.

Три вещи всплыли при отладке и стоят упоминания: D1 отдаёт числа параметров
вещественными, поэтому `/` делил вещественно и карма уезжала в дробь (10.5) —
понадобился `CAST(... AS INTEGER)` на каждом параметре; `referred_by` ссылается
на сами `users`, и сброс базы в тестах спотыкался о собственный внешний ключ;
частые `wrangler d1 execute` по файлу, который держит живой `wrangler dev`,
роняют соединение — отсюда `waitForWorker()` после сброса.

---

## Что осталось руками

1. **Проверить в живом Telegram** пункты из «Не проверено» выше — это
   единственный способ: подписать initData можно только настоящим токеном
   бота, поэтому прод-API локальными тестами не покрыть. Главное непроверенное
   звено — оценка чужого дела вторым человеком по deep-link.
2. **Потолок и число проверяющих** менять здесь: `MAX_USERS` в
   `workers/wrangler.jsonc`, `REVIEWER_SLOTS` в `lib/karma/review.ts`.
   После правки — `npm run deploy:api` (для слотов ещё и `npm run deploy:app`).
